[project]
name = "incident-env"
version = "0.1.0"
description = "Incident Response OpenEnv"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn"
]

[project.scripts]
server = "server.app:main"
