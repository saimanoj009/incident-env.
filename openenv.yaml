name: incident-response-env

description: AI environment for handling DevOps incidents

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: easy
  - id: medium
  - id: hard